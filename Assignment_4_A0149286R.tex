\input{preamble.tex}

\usepackage{empheq}

\begin{document}

% Indicate your name and your student number here (e.g., A01xxx)
\header{Assignment 4}{Wira Azmoon Ahmad}{A0149286R}

%======================================
\section{General Induced Subgraph Game}
\begin{proof}
    We can consider the elements in the set of edges 
    $e\in E,e=\{a,b\} \subseteq N$ where $a,b$ are players.
    Then we can define the induced subgraph game to be
    a weighted sum of ultimatum games
    \[v(S)=\sum_{e\in E} w(e)u_e(S)\]
    where $u_e$ is the ultimatum game for the players $a,b$. 
    Then
    \begin{equation}
    \begin{aligned}
        Sh_i(v)&=\sum_{e\in E} w(e)Sh_i(u_e) \quad\text{(by linearity)}\\
        &= \sum_{e\in E_i} w(e)Sh_i(u_e) \quad\text{(by dummy property)}\\
        &= \sum_{e\in E_i} w(e)\cdot\frac{1}{2} 
        \quad\text{(by symmetry/efficiency)}\\
        &= \frac{1}{2} \sum_{e\in E_i} w(e)
    \end{aligned}
    \end{equation}
\end{proof}


%======================================
\newpage
\section{Shapley Value}
\subsection{$N=\{1,2,3\}$}
\begin{empheq}[left=\empheqlbrace]{align}
    x_1+x_2+x_3&=1\\
    x_i &\geq 0, \forall i\in N\\
    x_1+x_2 &\geq 1\\
    x_2+x_3 &\geq 1\\
    x_1+x_3 &\geq 0
\end{empheq}
From $(2),(3),(4),(5)$, we have
\[x_1=x_3=0\]
Then we must have $x_2=1$.
The core payoff vector is $\boldsymbol{x}=(0,1,0)$.\\
Considering player 3's contribution for various permutations,
\begin{equation}
\begin{aligned}
    123&: v(\{1,2,3\})-v(\{1,2\})=0\\
    132&: v(\{1,3\})-v(\{1\})=0\\
    213&: v(\{1,2,3\})-v(\{1,2\})=0\\
    231&: v(\{2,3\})-v(\{2\})=1\\
    312&: v(\{3\})=0\\
    321&: v(\{3\})=0\\
\end{aligned}
\end{equation}
Thus, $Sh_3=1/6$. By symmetry, $Sh_1=1/6$.
By efficiency,
\[Sh_2=1-\frac{1}{6}-\frac{1}{6}=\frac{2}{3}\]

\newpage
\subsection{Induced Subgraph Game}
It can be seen that $v(\{1,2,3,4\})=v(\{1,2,3\})=7$, so we must have $x_4=0$.
The remaining game can be represented as
\[v(\{1\})=v(\{2\})=v(\{3\})=0\]
\[v(\{1,2\})=v(\{1,3\})=2\]
\[v(\{2,3\})=3\]
\begin{empheq}[left=\empheqlbrace]{align}
    x_1+x_2+x_3&=7\\
    x_i &\geq 0, \forall i\in \{1,2,3,4\}\\
    x_1+x_2 &\geq 2\\
    x_2+x_3 &\geq 3\\
    x_1+x_3 &\geq 2
\end{empheq}
Solving, we have
\begin{equation}
\begin{aligned}
    0\leq x_1 \leq 4\\
    0\leq x_2 \leq 5\\
    0\leq x_3 \leq 4
\end{aligned}
\end{equation}
Note that not all combinations are valid, the values chosen for
the core payoff vector must still satisfy the constraints above.
One example is 
\[\boldsymbol{x}=(2,2,3,0)\]
For the Shapley values, the result from Question 1 can be used.
Then,
\begin{equation}
\begin{aligned}
    Sh_1&=\frac{1}{2}(2+2)=2\\
    Sh_2&=\frac{1}{2}(2+3)=2.5\\
    Sh_3&=2.5\\
    Sh_4&=0
\end{aligned}
\end{equation}

\subsection{Weighted Voting Game}
Let $N=\{1,2,3,4,5,6\}$. Observe that we have
\[\forall i \in N : \sum_{j\in N\backslash\{i\}} w_j\geq 5\]
Thus, the game has no veto players. This game is also a simple game,
so by the theorem proven in the lectures slides, there does not exist a core.\\
Player 1 can only be pivotal when preceeded by players whose combined weight
is exactly 4. So they are preceeded by
\[\{2,6\},\{3,6\},\{2,3,4\},\{2,3,5\},\{4,5\}\]
By symmetry, $Sh_1=Sh_2=Sh_3$. Then
\[Sh_1=Sh_2=Sh_3=\frac{5\times 2!\times 3!}{6!}=\frac{1}{12}\]
Player 4 can only be pivotal when preceeded by players whose combined weight
is either 3 or 4. So they are preceeded by 
\[\{6\},\{1,5\},\{2,5\},\{3,5\},\{1,2,3\},
\{1,2,5\},\{1,3,5\},\{2,3,5\},\{1,6\},\{2,6\},\{3,6\}\]
By symmetry, $Sh_4=Sh_5$. Thus,
\[Sh_4=Sh_5=\frac{4!+10\times 2!\times 3!}{6!}=\frac{1}{5}\]
By efficiency, $\sum_{i\in N} Sh_i =1$. So
\[Sh_6=1-\frac{3}{12}-\frac{2}{5}=\frac{7}{20}\]

%======================================
\newpage
\section{Prove}
\subsection{}
False. A counterexample exists in the lecture slides:
\[v(1)=v(2)=v(3)=0\]
\[v(1,2)=4,v(1,3)=5,v(2,3)=6\]
\[v(1,2,3)=7\]
We have for all disjoint $S,T\subseteq \{1,2,3\}$,
\[v(S)+v(T)\leq v(S\cup T)\]
but its core is empty as proven in lecture.

\subsection{}
True. \\
\begin{proof}
    Suppose that $\vec{x},\vec{y}\in Core(\mathcal{G})$.
    From the definition of the $Core$,
    \[\forall S\subseteq N : \sum_{i\in S}x_i = x(S) \geq v(S)\]
    \[\forall S\subseteq N : \sum_{i\in S}y_i = y(S) \geq v(S)\]
    \[\forall S\subseteq N : 
    \sum_{i\in S}\alpha x_i + \sum_{i\in S}(1-\alpha) y_i
    \geq \alpha v(S) + (1-\alpha) v(S) = v(S)\]
    By definition, we have 
    $\alpha \vec{x} + (1-\alpha) \vec{y}\in Core(\mathcal{G})$.
\end{proof}

\subsection{}
True. \\
\begin{proof}
    By definition of the $Core$,
    \[\sum_{i\in N}x_i = v(N)\]
    \begin{equation}
        \forall i \in N : x_i \geq v(\{i\})
    \end{equation}
    By the Efficiency property of the Shapley value,
    \begin{equation}
        \sum_{i\in N} Sh_i = v(N) = \sum_{i\in N}x_i
    \end{equation}
    By the Additivity property, 
    \begin{equation}
        \forall i\in N : Sh_i(\{i\}) + Sh_i(N-\{i\}) = Sh_i
    \end{equation}
    By the Dummy property,
    \begin{equation}
        \forall i\in N : Sh_i(N-\{i\}) = 0
    \end{equation}
    Thus, from $(15), (17), (18)$,
    \begin{equation}
        \forall i\in N : Sh_i(\{i\}) = Sh_i = v(\{i\}) \geq x_i
    \end{equation}
    Suppose we have $\exists i \in N, Sh_i(N) > x_i$.
    From $(16)$,
    \[\sum_{i\in N} Sh_i > \sum_{i\in N}x_i\]
    A contradiction, so 
    \begin{equation}
        \forall i\in N : Sh_i(N) \leq x_i
    \end{equation}
    From $(19),(20)$, we have that
    \[\forall i\in N : Sh_i(N) = x_i\]
    If the core exists, the Shapley value is in the core.
\end{proof}

\subsection{}
True.\\
\begin{proof}
    For each subset $T\subseteq N$, there is some $WVG_T$ for which
    the task $t_j\in\mathcal{T}$ has the best value $v_j$.
    We can express the value of the $TTG$ as

    \[v(S) = \sum_{T\subseteq N, T\neq\emptyset} v_j WVG_{T}(S) \]
    Then we would have 
    \[Sh_i(v)=\sum_{T\subseteq N, T\neq\emptyset} v_j Sh_i(WVG_{T})\]
    That is, the Shapley value of the $TTG$ is a linear combination
    of the Shapley values of some $WVG$.
    It suffices to compare the Shapley value of the $WVG$.\\
    Consider players $i,j$, with weights $w_i>w_j$.
    Clearly, for any permutation where player $j$ is pivotal,
    $i$ will also be pivotal. Then, $Sh_i(WVG) \geq Sh_j(WVG)$.
    Thus, we have that if $w_i>w_j$, then $Sh_i\geq Sh_j$.
\end{proof}


\end{document}